import { BanksfilterPipe } from './banksfilter.pipe';

describe('BanksfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BanksfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
